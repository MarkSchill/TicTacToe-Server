# API Routes

Player routes:
- /player/:id (GET, POST)
-- Return the stats of the player record
- /player/join (POST)
-- Create a new player record with the given information

Game routes:
- /game/:id
- /game/start
- /game/finish
- /game/